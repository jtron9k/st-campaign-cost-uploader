# Secrets. ServiceTitan client ID / secret / app key / tenant IDs live here.
.env
.env.*
!.env.example
*.pem
credentials.json
secrets.*

# Real customer spend data. Keep anonymized fixtures in tests/fixtures/ instead.
/data/
/private/
*.local.xlsx
*.local.csv

# Session handoff and design mockups. Both name live tenants, campaign ids,
# and real spend figures, so they stay local. Keep them; just never commit them.
/next_steps.md
*.pen

# Per-tenant alias maps hold a real customer's campaign names. Share them
# with your team out of band, not through this public repo.
/aliases/

# Audit log of writes to production tenants. Local record, not shared history.
/logs/

# Claude Code local overrides
.claude/settings.local.json

# Node
node_modules/
dist/
build/
.next/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
